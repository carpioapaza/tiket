<% provide(:title, 'Edit Event') %>

<div class="container mt-4">
  <div class="row">
    <div class="col-md-12">
      <h1>Edit Event</h1>
      <%= render 'form' %>
      <%= link_to 'Back', users_events_path(@event), class: 'btn btn-secondary mt-2' %>
    </div>
  </div>
</div>
