<h1>Categorías</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= category.description %></td>
        <td><%= link_to 'Ver', super_admins_category_path(category) %></td>
        <td><%= link_to 'Editar', edit_super_admins_category_path(category) %></td>
        <td><%= link_to 'Eliminar', super_admins_category_path(category), method: :delete, data: { confirm: '¿Estás seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Categoria', new_super_admins_category_path, class: 'btn btn-primary' %>
