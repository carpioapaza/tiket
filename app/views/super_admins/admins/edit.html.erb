<% provide(:title, 'Edit Admin') %>

<div class="container mt-4">
  <div class="row">
    <div class="col-md-12">
      <h1>Edit Admin</h1>
      <%= render 'form' %>
      <%= link_to 'Back', super_admins_path(@event), class: 'btn btn-secondary mt-2' %>
    </div>
  </div>
</div>
