<% provide(:title, 'Edit Admin') %>

<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
      <h1 class="fw-bold" >Editar Admin</h1>
      <%= render 'form' %>
      <%= link_to 'Back', super_admins_admin_path(@admin), class: 'btn btn-secondary mt-2' %>
    </div>
  </div>
</div>
