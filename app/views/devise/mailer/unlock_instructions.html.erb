<div class="card">
  <div class="card-body">
    <h5 class="card-title">Hello <%= @resource.email %>!</h5>
    <p class="card-text">Your account has been locked due to an excessive number of unsuccessful sign in attempts.</p>
    <p class="card-text">Click the link below to unlock your account:</p>
    <p class="card-text"><%= link_to 'Unlock my account', unlock_url(@resource, unlock_token: @token) %></p>
  </div>
</div>
