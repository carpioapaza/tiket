<div class="card">
  <div class="card-body">
    <h5 class="card-title">Welcome <%= @email %>!</h5>
    <p class="card-text">You can confirm your account email through the link below:</p>
    <a href="<%= confirmation_url(@resource, confirmation_token: @token) %>" class="btn btn-primary">Confirm my account</a>
  </div>
</div>
